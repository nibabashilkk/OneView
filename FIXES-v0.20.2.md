# v0.20.2 — Home TopBar 状态收敛

## 问题

首页此前直接复用文档态 TopBar，导致在尚未打开文档时仍展示侧栏、打开文件、搜索等上下文操作，与首页已有的主 CTA 重复。

## 修复

TopBar 新增显式 `home | document` 模式：

- `home`：只保留可拖拽标题栏和全局“更多”；“更多”仅提供设置与主题。
- `document`：显示侧栏、Tab、新建/打开、搜索、插件工具项和完整更多菜单。
- 打开工作区但暂无活动文档时仍使用 `document` 模式，因为工作区侧栏属于有效上下文。

这不是按按钮逐个补条件，而是由 App 层根据当前上下文选择 Toolbar 模式。
