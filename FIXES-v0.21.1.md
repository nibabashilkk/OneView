# v0.21.1 — Interface & Usability Polish

## 目标

v0.21.0 完成了 Plugin Platform 与 Theme 系统的架构重构，但界面仍偏“功能面板集合”：入口隐藏、字号偏小、状态信息偏技术化、设置和插件中心缺少搜索与视觉反馈。v0.21.1 不改变核心架构，集中修复可发现性、信息层级和日常操作效率。

## 主要变化

### 首页

- 从居中的宣传式 Empty State 改成任务型工作台。
- “打开文件夹”作为主要工作流，“打开文件”作为次级工作流。
- 直接暴露命令面板、插件中心、偏好设置。
- 最近工作区 / 文件统一卡片语言，并增强 hover 反馈。

### 顶部与侧栏

- 命令面板与插件中心拥有固定入口，不再必须打开“更多”菜单。
- Workspace 将扫描耗时 / 缓存等诊断信息降级，保留用户真正关心的项目名与文档数量。
- 文件 / 搜索 / 目录采用统一图标分段控制。
- 搜索框带图标、清除按钮、结果摘要和友好空状态。

### 设置

- 左侧新增设置搜索，可用自然关键词定位分类。
- 分类项增加一句用途说明。
- “界面外观 / 插件主题 / 阅读主题”重新命名为“明暗模式 / 应用主题 / 阅读样式”，降低概念冲突。
- 应用主题与阅读样式增加缩略预览，不再只靠文字名称选择。

### 插件中心

- 增加插件 / 主题 / 格式搜索。
- 分类标签显示数量。
- 插件类型明确显示为“主题 · 无代码 / 文档查看器 / 扩展”。
- 运行状态变成状态 pill；插件 ID 降级为弱信息。
- 安全模型改为可展开说明；安全模式未开启时使用紧凑样式。

## 保持不变

- v0.21 Contribution-first Manifest、Theme Registry、Worker Runtime、安全边界与兼容层不变。
- Safe Mode 仍只暂停 Worker，声明式主题继续工作。
- Theme Bootstrap Cache、atomic replace、HTML export theme inheritance 不变。
