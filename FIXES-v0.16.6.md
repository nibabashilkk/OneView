# v0.16.6 — Default App Integration

- Contextual Markdown default-app prompt.
- macOS one-click default handler via NSWorkspace + UTType.
- Windows Default Apps deep-link + RegisteredApplications installer registration.
- Read-only current-handler detection on macOS and Windows.
- Settings UI for all supported developer document types.
