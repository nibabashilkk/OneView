[package]
name = "workspace-demo"
version = "0.17.3"
edition = "2021"

[features]
default = ["viewer"]
viewer = []
system-preview = []

[dependencies]
serde = { version = "1", features = ["derive"] }
